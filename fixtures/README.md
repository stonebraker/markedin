# Shared test fixtures

Canonical `.mi` input files and expected outputs for cross-parser conformance testing.

When multiple parser implementations exist, each parser's test suite should read from this directory to verify consistent behavior across languages.
